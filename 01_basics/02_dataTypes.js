"use strict"; // treat all JS cd=ode as newer version

// alert(3+3) we are using node js not browser


// code readability should be high


let name = "aastha"
let age = 12
let isLoggedIn = true
let state;

// PRIMITIVE DATATYPES:
// number 2 to power 53
// bigint (trading, stock market, reddit(aeso mei use hoti h ye))
// string => "" (prefer double qoutes)
// boolean => true/false
// null => standalone value (representation of empty value) (type object)
// undefined => abi value not assigned
// symbol => unique

//object




console.log(typeof null);